Для использования buildx.sh на Windows необходимо сначала выполнить команду `docker buildx create --use`.

Инициализация базы данных происходит в скрипте `init`, вызываемом при запуске контейнера с сервисом. 